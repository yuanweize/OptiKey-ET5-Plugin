[English](PRIVACY.md) | [简体中文](PRIVACY.zh-CN.md)

# 隐私策略与生物特征数据处理规范 (Privacy Policy)

## 基本准则

无障碍辅助沟通软件承载着最高级别的用户隐私责任。渐冻症及闭锁综合征用户依赖此类软件输入个人私密信件、银行账户凭证、医疗咨询内容以及亲密对话。

`OptiKey-ET5-Plugin` 架构从根本上贯彻了 **数据最小化 (Data Minimization)** 与 **零持久化 (Zero Persistence)** 原则：

1. **纯易失性实时流式处理**：
   - 从 Tobii 硬件接收的注视点坐标仅在易失性内存中处理，其唯一目的在于即时映射屏幕光标并完成按键命中测试。
   - 坐标数据在完成屏幕点击命中判定后立即从内存中丢弃。
2. **注视点数据零本地落盘存储**：
   - 插件绝不向磁盘写入任何原始注视点、轨迹坐标、眼动热力图或高频时间戳。
   - 日志文件仅记录高级别生命周期事件（如 `Connected`、`Reconnecting`、`Disconnected` 以及脱敏错误码）。绝对**禁止记录原始 $(X, Y)$ 坐标和设备硬件地址**。
3. **零网络外发与零遥测**：
   - 插件代码中完全不包含任何遥测（Telemetry）、用户画像分析、埋点追踪或网络传输模块。
   - 不发起任何 HTTP、TCP 或 UDP 网络请求。
4. **诊断工具隐私脱敏**：
   - 硬件诊断工具（`ET5Diagnostics.exe`）仅输出聚合后的样本计数统计（例如 `5秒内接收到167个样本`）与驱动版本号。
   - 绝对**不输出、不记录用户注视点坐标**。
   - 设备硬件地址在诊断日志中均作了严格脱敏处理。
5. **符合即时交互使用规范**：
   - 严格遵循 `TOBII_FIELD_OF_USE_INTERACTIVE` 要求：注视数据仅用于实时界面交互控制，绝不转存，绝无二次商业使用。
