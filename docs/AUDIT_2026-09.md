# Independent Audit: 2026-09

Audit date: 2026-09-21. Results below describe the hardened branch and its recorded CI evidence.

## Executive result

Readiness is **NOT READY**. GitHub currently shows no releases and no tags. The normal build workflow uploads an Actions artifact; the release workflow is tag-triggered and requires a prerelease tag. ET5 hardware and Tobii runtime remain unavailable for validation.

## Claim verification matrix

| Claim | Evidence | Result | Severity | Required action | Fixed commit |
| --- | --- | --- | --- | --- | --- |
| GitHub Releases is empty because release creation is tag-triggered | GitHub releases/tags pages; `.github/workflows/release.yml` | VERIFIED | P2 | Keep release creation tag-only and document status | None; working tree |
| Release action can publish with repository contents write permission | Release workflow had no job permission block | PARTIALLY VERIFIED | P1 | Added explicit `contents: write` | None; working tree |
| Device-info ABI is safe | `TobiiStreamEngineBinding.cs` had an unverified fixed-string struct | UNSUPPORTED | P0 | Removed the struct and interface surface; do not reintroduce without authoritative ABI proof | Current branch |
| Callback wait accepts a timeout | Native delegate accepts only count and device array; managed interface added timeout | FALSE | P0 | Removed managed timeout parameter and timeout branch | None; working tree |
| Stop cannot destroy native state while worker runs | `Stop()` joined for 1500 ms, then cleanup continued | FALSE | P0 | Stop now waits for worker exit before cleanup | None; working tree |
| First enumerated device is ET5 | `TobiiGazeProvider` used `deviceUrls[0]` | UNSUPPORTED | P0 | Fail closed when identity inspection is unavailable | None; working tree |
| Runtime discovery includes registry probing | `TobiiRuntimeLocator.cs` only constructed fixed paths | FALSE | P1 | Inventory script added separately; implement registry discovery only from observed Windows evidence | None; working tree |
| Authenticode is verified | `X509Certificate.CreateFromSignedFile` plus subject substring | FALSE | P0 | Do not call metadata inspection trust validation; implement WinVerifyTrust before enabling mandatory trust policy | Not fixed |
| Coordinate mapping is pixel-accurate at all DPI levels | Only synthetic `DisplayMetrics` tests exist; Win32 query is primary-screen only | UNSUPPORTED | P1 | Run Windows DPI, monitor, and resolution validation; remove silent fallback | None |
| OptiKey 4.1.1, 4.2.0, 4.2.1, and 4.2.2 are verified | Compatibility table was not backed by exact-version loader runs | FALSE | P1 | Marked versions unverified except contract reference | None; working tree |
| Manual installation uses `%LocalAppData%` | README stated LocalAppData | FALSE | P1 | Corrected English and Chinese docs to `%AppData%` | None; working tree |
| CI package is version `1.0.0` | `build-and-test.yml` passed `1.0.0` to package script | VERIFIED | P1 | CI now uses `0.0.0-ci.<run>`; script default is non-release | None; working tree |
| License is canonical GPL-3.0-only text | `LICENSE` now matches the SPDX GPL-3.0-only source used for replacement | VERIFIED | P1 | Preserve the canonical file and use GPL-3.0-only terminology | Current branch |
| NUnit tests executed in CI | Historical VSTest step reported no tests available | FALSE for historical run | P0 | Use project-level dotnet test, parse TRX, and fail on zero executed tests | Current branch; rerun required |
| Packaged loader used target CLR | Historical PowerShell harness failed to load System.Reactive under CoreCLR | FALSE for historical run | P0 | Use the net46 x64 console harness against host dependencies | Current branch; rerun required |
| Tobii use is permitted / fully compliant | `RUNTIME_RESEARCH.md` asserted this without current policy confirmation | FALSE | P1 | Reworded as unresolved and retain developer-relations inquiry | None; working tree |

## ABI provenance table

The table records the declarations actually used by the binding. The official documentation URL is a lead, not proof of a particular installed runtime. The repository did not contain a versioned public header or an independent authoritative ABI artifact, so confidence is deliberately low or medium.

| Symbol | Local C# signature | Source URL/document | API/version | Verified date | Confidence |
| --- | --- | --- | --- | --- | --- |
| `tobii_error_t` | C# enum | `https://developer.tobii.com/consumer-eye-trackers/stream-engine/`; `docs/RUNTIME_RESEARCH.md` | Stream Engine version unknown | 2026-09-21 | Low |
| `tobii_validity_t` | C# enum | Same official documentation lead | Version unknown | 2026-09-21 | Low |
| `tobii_field_of_use_t` | C# enum | Same official documentation lead | Version unknown | 2026-09-21 | Low |
| `tobii_gaze_point_t` | Sequential struct: `long`, enum, `float`, `float` | Same official documentation lead | Version unknown | 2026-09-21 | Low |
| `tobii_device_info_t` | Removed from managed source | No authoritative header obtained | Not used | 2026-09-21 | None |
| `tobii_api_create` | Cdecl `(out IntPtr, IntPtr, IntPtr)` | Official documentation lead; local research document | Version unknown | 2026-09-21 | Low |
| `tobii_api_destroy` | Cdecl `(IntPtr)` | Same | Version unknown | 2026-09-21 | Low |
| `tobii_enumerate_local_device_urls` | Cdecl `(IntPtr, receiver, IntPtr)` | Same | Version unknown | 2026-09-21 | Low |
| `tobii_device_create` | Cdecl `(IntPtr, string, enum, out IntPtr)` | Same | Version unknown | 2026-09-21 | Low |
| `tobii_device_destroy` | Cdecl `(IntPtr)` | Same | Version unknown | 2026-09-21 | Low |
| `tobii_device_reconnect` | Cdecl `(IntPtr)` | Same | Version unknown | 2026-09-21 | Low |
| `tobii_wait_for_callbacks` | Cdecl `(IntPtr count, IntPtr[] devices)` | Same; local signature agrees with research text | Version unknown | 2026-09-21 | Medium |
| `tobii_device_process_callbacks` | Cdecl `(IntPtr)` | Same | Version unknown | 2026-09-21 | Low |
| `tobii_gaze_point_subscribe` | Cdecl `(IntPtr, callback, IntPtr)` | Same | Version unknown | 2026-09-21 | Low |
| `tobii_gaze_point_unsubscribe` | Cdecl `(IntPtr)` | Same | Version unknown | 2026-09-21 | Low |
| `tobii_get_device_info` | Cdecl declaration removed from active binding | Same; no struct proof | Disabled | 2026-09-21 | None |
| `tobii_error_message` | Cdecl `(enum) -> IntPtr` | Same | Version unknown | 2026-09-21 | Low |

Historical OptiKey source was not copied. It may be used only as a discrepancy reference, and it is not an acceptable provenance source for this table.

## Remaining P0/P1 work

- Obtain the exact legitimate Stream Engine ABI documentation/header for the runtime version, including `tobii_device_info_t`, callback lifetime rules, and device identity fields.
- Replace certificate metadata inspection with real WinVerifyTrust validation, including integrity and chain policy, before treating signatures as a security gate.
- Design a non-heuristic ET5 identity check from documented device information; until then, keep production fail-closed.
- Prove a bounded shutdown mechanism for the native callback wait, or retain a shutdown design that cannot destroy native state before the wait returns and test it on Windows.
- Obtain a successful run of the net46 OptiKey-like loader smoke test against the exact packaged ZIP.
- Obtain a successful TRX test gate with total and executed counts greater than zero.
- Preserve the exact selected canonical GPL-3.0-only text.
- Obtain Tobii Developer Relations/legal clarification for consumer ET5 assistive use.

## Exact validation performed

- Editor diagnostics: no errors reported for the edited C# binding, runtime, and provider files.
- `dotnet test OptiKey-ET5-Plugin.sln --configuration Release --no-restore`: not run successfully because `dotnet` is not installed on this macOS host.
- Windows Actions run `35545794228` (`654790d`) passed both pinned-stable and upstream-main build/test matrix legs.
- Windows Actions run `35578818780` (`23f967a`) succeeded for both matrix legs. Pinned-stable job `106266684068` passed Contracts build, restore, x64 Release build, real NUnit execution, package generation, ZIP audit, net46 packaged-loader harness, and artifact upload. Upstream-main job `106266683818` passed Contracts build, restore, x64 Release build, and real NUnit execution.
- The pinned-stable TRX output reports `total=26 executed=26 passed=26 failed=0`; skipped is `0` because all 26 tests executed. Upstream-main reports the same counters.
- Final package artifact `OptiKey-ET5-Plugin-Package` has ID `10629865300`, size `27620` bytes, and GitHub artifact digest `sha256:86d5786ea9f1a87382491b3ad293758cfccfc879e4023370cbd59222c83a035e`.
- The inner ZIP is `OptiKey-ET5-Plugin-v0.0.0-ci.21.zip` with SHA256 `4fb775bba8d822cf249f50578d24883f7d88c90606e0ab4f786f0caedb738c24`. `SHA256SUMS.txt` contains the same hash.
- Complete ZIP inventory: `LICENSE`; `OptiKey.ET5.Plugin.dll`. No Tobii DLL, Contracts DLL, test DLL, synthetic DLL, PDB, log4net DLL, or System.Reactive DLL is present.
- The managed plugin is a PE32+ x86-64 .NET assembly. Its metadata references host-provided `System.Reactive` and `JuliusSweetland.OptiKey.Contracts`; those assemblies are intentionally not redistributed.
- Authenticated artifact download and log inspection were performed with GitHub CLI. The final loader log reports `PACKAGED LOADER SMOKE TEST PASSED` before normal process cleanup.
- GitHub release page inspection: no releases shown.
- GitHub tags page inspection: no tags shown.
- No CI URL/result or artifact SHA256 can honestly be reported from this environment.
